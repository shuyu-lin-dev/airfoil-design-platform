repos:
  - repo: local
    hooks:
      - id: file-and-func-limits
        name: Check scoped files and Python size limits
        entry: python3 .harness/tool/pre-commit-check.py
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
