# Learnings — 2026-05-30 项目自检 #2

## 发现

### 代码结构违规未修复的延续

2026-05-30 全量审计发现的 `core/geometry.py` 355 行和 `generate_wing_3d_step()` 62 行两项违规，
在审计后的 P0/P1/P2 优化 session 中未被修复——优化专注于 harness 流程改进，未涉及代码拆分。
自检 #2 才实际执行拆分。

**教训**：审计发现违规后，应在同一 session 或下一个 session 立即修复，不应让已知违规跨越多个 session。

### 新增违规

`coupled_optimize()` 61 行和 3 个测试文件 >200 行是全量审计中未发现的违规。
审计脚本只检查了 `core/geometry.py`，未遍历所有源文件。

**教训**：审计应遍历所有文件，不能只检查已知文件。

### sprint-contracts 补建策略

12 个缺失的轻量合同使用 `_TEMPLATE-light.md` 格式（5 行 YAML frontmatter）一次性创建。
轻量模板足够覆盖已完成任务——这些任务已经是 passing，合同的作用是审计追溯，不是指导新实现。

### pre-commit 检查覆盖

当前 pre-commit hook 只检查 `backend/src/` 和 `backend/tests/` 下的 `.py` 文件。
`.harness/tool/` 下的脚本不在检查范围。如 `pre-commit-check.py` 本身 104 行（在 200 行限制内），但 harness 辅助脚本是否适用代码结构约束未明确。

### 剩余未修复项

以下项目是环境/基础设施问题，修复需要用户确认（新依赖、系统级变更）：
1. Python 3.10.12 → 3.11+（需要系统升级或 pyenv）
2. 创建 .venv 并迁移依赖（需要用户确认环境隔离策略）
3. `serialization/`、`models/`、`optimization/` 模块空壳（等待后续阶段填充）
